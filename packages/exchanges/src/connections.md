если подписка содержит в себе множество streams, и они распределяются по нескольким
подключениям, то может быть такая ситуация, при которой часть streams будет реализована
успешно по одним подключениям, а другая часть распределенная по другому подключению,
вывовет в нем сбой.

в случае если сбой происходит во всех подключениях, и у нас есть партия реализованных запросов
и партия "зависших" в состоянии подключения

обработка подписки содержащая потоки на 3 подключения:
в 1 - часть реализованных запросов
в 2 - часть реализованных запросов и часть нереализованных запросов (сбой)
в 3 - часть нереализованных запросов (сбой)

часть реализованных запросов нужно оставить, другую же воспроизвести заново
все нереализованные запросы нужно реализовать
