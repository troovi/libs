import { createLayout } from '@troovi/chezit'

export const Splitter = createLayout(() => {
  return () => {
    return <div className="w-full" style={{ border: '0.5px solid rgb(224 224 224)' }} />
  }
})
